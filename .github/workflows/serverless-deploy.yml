name: serverless-deploy
on:
  workflow_call:
    inputs:
      stage:
        description: Environment stage to deploy to e.g. dev, production
        required: false
        type: string
        default: dev

jobs:
  deploy:
    name: deploy
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [20.x]
    steps:
    - uses: actions/checkout@v3
    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v3
      with:
        node-version: ${{ matrix.node-version }}
    - run: npm i -g serverless
    - run: npm ci
    - run: serverless config credentials --provider aws --key ${{ secrets.DEPLOYER_AWS_ACCESS_KEY_ID }} --secret ${{ secrets.DEPLOYER_AWS_SECRET_ACCESS_KEY }} -o
    - name: serverless deploy
      run: sls deploy --stage=${{ inputs.stage }}
      env:
        SERVERLESS_ACCESS_KEY: ${{ secrets.SERVERLESS_ACCESS_KEY }}